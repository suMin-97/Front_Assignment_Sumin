export interface ColumnStyledProps {
  $isForbidden: boolean;
  $isDraggingOver: boolean;
}

export interface ItemStyledProps {
  $isForbidden: boolean;
  $isUsingDrag: boolean;
  $isDragging: boolean;
  $isSelected: boolean;
}
